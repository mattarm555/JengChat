#pragma once
#include "../app_state.h"
void OpenOnlineUsers(AppState& app);
void OpenPlayerInvite(AppState& app, GameView game);
void DrawOnlineUsers(AppState& app, int canvasWidth = 1200, int canvasHeight = 650);
